version: "3.9"

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://pubai:pubai@db:5432/pubai
      - REDIS_URL=redis://redis:6379/0
      - AI_PROVIDER=${AI_PROVIDER:-claude}
      - AI_API_KEY=${AI_API_KEY:-}
      - AI_MODEL=${AI_MODEL:-claude-sonnet-4-20250514}
      - OLLAMA_HOST=${OLLAMA_HOST:-http://host.docker.internal:11434}
      - SECRET_KEY=${SECRET_KEY:-dev-secret-change-me}
      - CORS_ORIGINS=["http://localhost:3000"]
      - WORKSPACE_ENABLED=${WORKSPACE_ENABLED:-true}
      - WORKSPACE_IMAGE=${WORKSPACE_IMAGE:-pubai-workspace:latest}
      - WORKSPACE_PRIVILEGED=${WORKSPACE_PRIVILEGED:-true}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - pubai

  web:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    command: sh -c "npm install && npm run dev"
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - pubai

  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=pubai
      - POSTGRES_PASSWORD=pubai
      - POSTGRES_DB=pubai
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pubai -d pubai"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - pubai

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - pubai

  workspace-builder:
    build:
      context: ./docker
      dockerfile: Dockerfile.workspace
    image: pubai-workspace:latest
    profiles: ["build"]
    command: "echo 'Workspace image built'"

networks:
  pubai:
    driver: bridge

volumes:
  pgdata:
  frontend_node_modules:
